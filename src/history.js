// File to create and maintain a history object to be used throughout the app - for easier access and modify what page the user is looking at.

import { createBrowserHistory } from 'history';

export default createBrowserHistory();